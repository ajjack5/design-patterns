namespace DesignPatterns.Behavioural.ChainOfResponsibility.CSharp.Examples
{
    public class Example
    {
        // Most common usages are for things such as global exception handlers, auth handlers, HTTP handlers, etc...
        // Most, if not all, already have defined middleware or implementations for specific architecutres they're used in
    }
}
